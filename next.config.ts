import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Ainda nada aqui. Os cabeçalhos de segurança entram quando houver
     conteúdo para proteger — ver docs/PLANO.md. */
};

export default nextConfig;
